query CategoryGetAllProducts($slug: String) {
  categories(where: { slug_contains: $slug }) {
    name
    products {
      id
      name
      description
      images(first: 4) {
        url
      }
      price
    }
  }
}
